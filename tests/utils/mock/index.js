/* eslint-disable global-require */
const index = {
    BucketD: require('./BucketD'),
    Vault: require('./Vault'),
    log: require('./log'),
    values: require('./values'),
};

module.exports = index;
