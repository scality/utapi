general:
    branches:
        ignore:
            - /^ultron\/.*/ # Ignore ultron/* branches

machine:
    node:
        version: 4.2
test:
    override:
        - npm run --silent lint_md
        - npm run --silent lint
